{
  "_comment": "Merge the 'hooks' block into ~/.claude/settings.json. Each event POSTs its JSON (stdin) to the feldd-cc daemon. The daemon replies 200 instantly so it never stalls a turn. Verify event names against your Claude Code version (run the capture config first); adjust matcher syntax if your version requires it.",
  "hooks": {
    "SessionStart":     [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "SessionEnd":       [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "UserPromptSubmit": [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "PreToolUse":       [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "PostToolUse":      [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "Notification":     [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "PermissionRequest":[{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "SubagentStart":    [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "SubagentStop":     [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }],
    "Stop":             [{ "hooks": [{ "type": "command", "command": "curl -s --max-time 2 -X POST \"http://localhost:9200/hook?pane=$TMUX_PANE\" -d @-" }] }]
  }
}
